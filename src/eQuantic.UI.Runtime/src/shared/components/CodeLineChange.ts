import { $eq, CodeInnerChange } from "../runtime-exports";

export class CodeLineChange { declare originalStart: number; declare originalCount: number; declare modifiedStart: number; declare modifiedCount: number; declare inner: CodeInnerChange[]; constructor(originalStart: any = 0, originalCount: any = 0, modifiedStart: any = 0, modifiedCount: any = 0, inner: any = null) { this.originalStart = originalStart; this.originalCount = originalCount; this.modifiedStart = modifiedStart; this.modifiedCount = modifiedCount; this.inner = inner; } equals(o: unknown) { return o instanceof CodeLineChange && $eq.equals(this.originalStart, o.originalStart) && $eq.equals(this.originalCount, o.originalCount) && $eq.equals(this.modifiedStart, o.modifiedStart) && $eq.equals(this.modifiedCount, o.modifiedCount) && $eq.equals(this.inner, o.inner); } with(patch: any) { return new CodeLineChange(('originalStart' in patch ? patch.originalStart : this.originalStart), ('originalCount' in patch ? patch.originalCount : this.originalCount), ('modifiedStart' in patch ? patch.modifiedStart : this.modifiedStart), ('modifiedCount' in patch ? patch.modifiedCount : this.modifiedCount), ('inner' in patch ? patch.inner : this.inner)); } toString() { return `CodeLineChange { OriginalStart = ${this.originalStart}, OriginalCount = ${this.originalCount}, ModifiedStart = ${this.modifiedStart}, ModifiedCount = ${this.modifiedCount}, Inner = ${this.inner} }`; } }

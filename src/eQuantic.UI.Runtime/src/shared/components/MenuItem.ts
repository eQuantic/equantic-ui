import { $eq } from "../runtime-exports";

export class MenuItem { declare label: string; declare icon: string | null; declare destructive: boolean; declare disabled: boolean; constructor(label: any = null, icon: any = null, destructive: any = false, disabled: any = false) { this.label = label; this.icon = icon; this.destructive = destructive; this.disabled = disabled; } equals(o: unknown) { return o instanceof MenuItem && $eq.equals(this.label, o.label) && $eq.equals(this.icon, o.icon) && $eq.equals(this.destructive, o.destructive) && $eq.equals(this.disabled, o.disabled); } with(patch: any) { return new MenuItem(('label' in patch ? patch.label : this.label), ('icon' in patch ? patch.icon : this.icon), ('destructive' in patch ? patch.destructive : this.destructive), ('disabled' in patch ? patch.disabled : this.disabled)); } toString() { return `MenuItem { Label = ${this.label}, Icon = ${this.icon}, Destructive = ${this.destructive}, Disabled = ${this.disabled} }`; } }

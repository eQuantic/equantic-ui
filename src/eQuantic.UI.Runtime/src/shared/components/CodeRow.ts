import { $eq, CodeRowKindValue } from "../runtime-exports";

export class CodeRow { declare kind: CodeRowKindValue; declare line: number; declare count: number; declare sourceLine: number; declare label: string | null; constructor(kind: any = 'line', line: any = 0, count: any = 1, sourceLine: any = -1, label: any = null) { this.kind = kind; this.line = line; this.count = count; this.sourceLine = sourceLine; this.label = label; } equals(o: unknown) { return o instanceof CodeRow && $eq.equals(this.kind, o.kind) && $eq.equals(this.line, o.line) && $eq.equals(this.count, o.count) && $eq.equals(this.sourceLine, o.sourceLine) && $eq.equals(this.label, o.label); } with(patch: any) { return new CodeRow(('kind' in patch ? patch.kind : this.kind), ('line' in patch ? patch.line : this.line), ('count' in patch ? patch.count : this.count), ('sourceLine' in patch ? patch.sourceLine : this.sourceLine), ('label' in patch ? patch.label : this.label)); } toString() { return `CodeRow { Kind = ${this.kind}, Line = ${this.line}, Count = ${this.count}, SourceLine = ${this.sourceLine}, Label = ${this.label} }`; } }

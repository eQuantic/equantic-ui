import { $eq } from "../runtime-exports";

export class NavItem { declare icon: string; declare label: string; declare selectedIcon: string | null; declare badgeCount: number; constructor(icon: any = null, label: any = null, selectedIcon: any = null, badgeCount: any = 0) { this.icon = icon; this.label = label; this.selectedIcon = selectedIcon; this.badgeCount = badgeCount; } equals(o: unknown) { return o instanceof NavItem && $eq.equals(this.icon, o.icon) && $eq.equals(this.label, o.label) && $eq.equals(this.selectedIcon, o.selectedIcon) && $eq.equals(this.badgeCount, o.badgeCount); } with(patch: any) { return new NavItem(('icon' in patch ? patch.icon : this.icon), ('label' in patch ? patch.label : this.label), ('selectedIcon' in patch ? patch.selectedIcon : this.selectedIcon), ('badgeCount' in patch ? patch.badgeCount : this.badgeCount)); } toString() { return `NavItem { Icon = ${this.icon}, Label = ${this.label}, SelectedIcon = ${this.selectedIcon}, BadgeCount = ${this.badgeCount} }`; } }

import { $eq } from "../runtime-exports";

export class CodeDiffGap { declare originalLine: number; declare modifiedLine: number; declare originalCount: number; declare modifiedCount: number; declare header: string; constructor(originalLine: any = 0, modifiedLine: any = 0, originalCount: any = 0, modifiedCount: any = 0, header: any = null) { this.originalLine = originalLine; this.modifiedLine = modifiedLine; this.originalCount = originalCount; this.modifiedCount = modifiedCount; this.header = header; } equals(o: unknown) { return o instanceof CodeDiffGap && $eq.equals(this.originalLine, o.originalLine) && $eq.equals(this.modifiedLine, o.modifiedLine) && $eq.equals(this.originalCount, o.originalCount) && $eq.equals(this.modifiedCount, o.modifiedCount) && $eq.equals(this.header, o.header); } with(patch: any) { return new CodeDiffGap(('originalLine' in patch ? patch.originalLine : this.originalLine), ('modifiedLine' in patch ? patch.modifiedLine : this.modifiedLine), ('originalCount' in patch ? patch.originalCount : this.originalCount), ('modifiedCount' in patch ? patch.modifiedCount : this.modifiedCount), ('header' in patch ? patch.header : this.header)); } toString() { return `CodeDiffGap { OriginalLine = ${this.originalLine}, ModifiedLine = ${this.modifiedLine}, OriginalCount = ${this.originalCount}, ModifiedCount = ${this.modifiedCount}, Header = ${this.header} }`; } }

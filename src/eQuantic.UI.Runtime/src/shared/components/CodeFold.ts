import { $eq } from "../runtime-exports";

export class CodeFold { declare startLine: number; declare endLine: number; declare placeholder: string | null; constructor(startLine: any = 0, endLine: any = 0, placeholder: any = null) { this.startLine = startLine; this.endLine = endLine; this.placeholder = placeholder; } equals(o: unknown) { return o instanceof CodeFold && $eq.equals(this.startLine, o.startLine) && $eq.equals(this.endLine, o.endLine) && $eq.equals(this.placeholder, o.placeholder); } with(patch: any) { return new CodeFold(('startLine' in patch ? patch.startLine : this.startLine), ('endLine' in patch ? patch.endLine : this.endLine), ('placeholder' in patch ? patch.placeholder : this.placeholder)); } toString() { return `CodeFold { StartLine = ${this.startLine}, EndLine = ${this.endLine}, Placeholder = ${this.placeholder} }`; } }

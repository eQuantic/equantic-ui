import { $eq } from "../runtime-exports";

export class CultureOption { declare name: string; declare label: string; declare short: string | null; declare flag: string | null; constructor(name: any = null, label: any = null, short: any = null, flag: any = null) { this.name = name; this.label = label; this.short = short; this.flag = flag; } equals(o: unknown) { return o instanceof CultureOption && $eq.equals(this.name, o.name) && $eq.equals(this.label, o.label) && $eq.equals(this.short, o.short) && $eq.equals(this.flag, o.flag); } with(patch: any) { return new CultureOption(('name' in patch ? patch.name : this.name), ('label' in patch ? patch.label : this.label), ('short' in patch ? patch.short : this.short), ('flag' in patch ? patch.flag : this.flag)); } toString() { return `CultureOption { Name = ${this.name}, Label = ${this.label}, Short = ${this.short}, Flag = ${this.flag} }`; } }

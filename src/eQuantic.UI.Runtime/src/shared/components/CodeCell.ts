import { $eq } from "../runtime-exports";

export class CodeCell { declare start: number; declare end: number; declare cell: number; declare width: number; constructor(start: any = 0, end: any = 0, cell: any = 0, width: any = 0) { this.start = start; this.end = end; this.cell = cell; this.width = width; } equals(o: unknown) { return o instanceof CodeCell && $eq.equals(this.start, o.start) && $eq.equals(this.end, o.end) && $eq.equals(this.cell, o.cell) && $eq.equals(this.width, o.width); } with(patch: any) { return new CodeCell(('start' in patch ? patch.start : this.start), ('end' in patch ? patch.end : this.end), ('cell' in patch ? patch.cell : this.cell), ('width' in patch ? patch.width : this.width)); } toString() { return `CodeCell { Start = ${this.start}, End = ${this.end}, Cell = ${this.cell}, Width = ${this.width} }`; } }

import { $eq } from "../runtime-exports";

export class CodeCollapse { declare firstLine: number; declare lastLine: number; declare placeholder: boolean; declare label: string | null; constructor(firstLine: any = 0, lastLine: any = 0, placeholder: any = true, label: any = null) { this.firstLine = firstLine; this.lastLine = lastLine; this.placeholder = placeholder; this.label = label; } equals(o: unknown) { return o instanceof CodeCollapse && $eq.equals(this.firstLine, o.firstLine) && $eq.equals(this.lastLine, o.lastLine) && $eq.equals(this.placeholder, o.placeholder) && $eq.equals(this.label, o.label); } with(patch: any) { return new CodeCollapse(('firstLine' in patch ? patch.firstLine : this.firstLine), ('lastLine' in patch ? patch.lastLine : this.lastLine), ('placeholder' in patch ? patch.placeholder : this.placeholder), ('label' in patch ? patch.label : this.label)); } toString() { return `CodeCollapse { FirstLine = ${this.firstLine}, LastLine = ${this.lastLine}, Placeholder = ${this.placeholder}, Label = ${this.label} }`; } }

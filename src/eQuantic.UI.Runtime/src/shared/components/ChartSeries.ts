import { $eq } from "../runtime-exports";

export class ChartSeries { declare name: string; declare values: number[]; declare slot: number; constructor(name: any = null, values: any = null, slot: any = null) { this.name = name; this.values = values; this.slot = slot; } equals(o: unknown) { return o instanceof ChartSeries && $eq.equals(this.name, o.name) && $eq.equals(this.values, o.values) && $eq.equals(this.slot, o.slot); } with(patch: any) { return new ChartSeries(('name' in patch ? patch.name : this.name), ('values' in patch ? patch.values : this.values), ('slot' in patch ? patch.slot : this.slot)); } slotAt(position: number) { return this.slot >= 0 ? this.slot : position; } at(category: number) { return category < this.values.length ? this.values[category] : 0; } toString() { return `ChartSeries { Name = ${this.name}, Values = ${this.values}, Slot = ${this.slot} }`; } }

import { $eq, TypeStyle } from "../runtime-exports";

export class CodeMetrics { declare style: TypeStyle; declare lineHeight: number; declare columnWidth: number; declare gutterWidth: number; constructor(style: any = null, lineHeight: any = 0, columnWidth: any = 0, gutterWidth: any = 0) { this.style = style; this.lineHeight = lineHeight; this.columnWidth = columnWidth; this.gutterWidth = gutterWidth; } equals(o: unknown) { return o instanceof CodeMetrics && $eq.equals(this.style, o.style) && $eq.equals(this.lineHeight, o.lineHeight) && $eq.equals(this.columnWidth, o.columnWidth) && $eq.equals(this.gutterWidth, o.gutterWidth); } with(patch: any) { return new CodeMetrics(('style' in patch ? patch.style : this.style), ('lineHeight' in patch ? patch.lineHeight : this.lineHeight), ('columnWidth' in patch ? patch.columnWidth : this.columnWidth), ('gutterWidth' in patch ? patch.gutterWidth : this.gutterWidth)); } get contentLeft() { return this.gutterWidth + 12; } get contentTop() { return 12; } toString() { return `CodeMetrics { Style = ${this.style}, LineHeight = ${this.lineHeight}, ColumnWidth = ${this.columnWidth}, GutterWidth = ${this.gutterWidth} }`; } }

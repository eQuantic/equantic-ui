import { $eq, CodePatchLine } from "../runtime-exports";

export class CodePatchHunk { declare originalStart: number; declare originalCount: number; declare modifiedStart: number; declare modifiedCount: number; declare section: string; declare lines: CodePatchLine[]; declare header: string; constructor(originalStart: any = 0, originalCount: any = 0, modifiedStart: any = 0, modifiedCount: any = 0, section: any = null, lines: any = null, header: any = '') { this.originalStart = originalStart; this.originalCount = originalCount; this.modifiedStart = modifiedStart; this.modifiedCount = modifiedCount; this.section = section; this.lines = lines; this.header = header; } equals(o: unknown) { return o instanceof CodePatchHunk && $eq.equals(this.originalStart, o.originalStart) && $eq.equals(this.originalCount, o.originalCount) && $eq.equals(this.modifiedStart, o.modifiedStart) && $eq.equals(this.modifiedCount, o.modifiedCount) && $eq.equals(this.section, o.section) && $eq.equals(this.lines, o.lines) && $eq.equals(this.header, o.header); } with(patch: any) { return new CodePatchHunk(('originalStart' in patch ? patch.originalStart : this.originalStart), ('originalCount' in patch ? patch.originalCount : this.originalCount), ('modifiedStart' in patch ? patch.modifiedStart : this.modifiedStart), ('modifiedCount' in patch ? patch.modifiedCount : this.modifiedCount), ('section' in patch ? patch.section : this.section), ('lines' in patch ? patch.lines : this.lines), ('header' in patch ? patch.header : this.header)); } toString() { return `CodePatchHunk { OriginalStart = ${this.originalStart}, OriginalCount = ${this.originalCount}, ModifiedStart = ${this.modifiedStart}, ModifiedCount = ${this.modifiedCount}, Section = ${this.section}, Lines = ${this.lines}, Header = ${this.header} }`; } }

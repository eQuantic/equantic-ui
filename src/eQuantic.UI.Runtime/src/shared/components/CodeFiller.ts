import { $eq } from "../runtime-exports";

export class CodeFiller { declare beforeLine: number; declare rows: number; declare sourceLine: number; declare label: string | null; constructor(beforeLine: any = 0, rows: any = 0, sourceLine: any = -1, label: any = null) { this.beforeLine = beforeLine; this.rows = rows; this.sourceLine = sourceLine; this.label = label; } equals(o: unknown) { return o instanceof CodeFiller && $eq.equals(this.beforeLine, o.beforeLine) && $eq.equals(this.rows, o.rows) && $eq.equals(this.sourceLine, o.sourceLine) && $eq.equals(this.label, o.label); } with(patch: any) { return new CodeFiller(('beforeLine' in patch ? patch.beforeLine : this.beforeLine), ('rows' in patch ? patch.rows : this.rows), ('sourceLine' in patch ? patch.sourceLine : this.sourceLine), ('label' in patch ? patch.label : this.label)); } toString() { return `CodeFiller { BeforeLine = ${this.beforeLine}, Rows = ${this.rows}, SourceLine = ${this.sourceLine}, Label = ${this.label} }`; } }

import { $eq, CodeRange, ColorToken } from "../runtime-exports";

export class CodeDecoration { declare range: CodeRange; declare kind: string; declare color: ColorToken | null; constructor(range: any = null, kind: any = 'highlight', color: any = null) { this.range = range; this.kind = kind; this.color = color; } equals(o: unknown) { return o instanceof CodeDecoration && $eq.equals(this.range, o.range) && $eq.equals(this.kind, o.kind) && $eq.equals(this.color, o.color); } with(patch: any) { return new CodeDecoration(('range' in patch ? patch.range : this.range), ('kind' in patch ? patch.kind : this.kind), ('color' in patch ? patch.color : this.color)); } toString() { return `CodeDecoration { Range = ${this.range}, Kind = ${this.kind}, Color = ${this.color} }`; } }

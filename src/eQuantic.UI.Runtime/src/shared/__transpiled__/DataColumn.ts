import { $eq, GridTrack } from "@equantic/runtime";

export class DataColumn { declare header: string; declare track: GridTrack; declare align: string; declare sortable: boolean; constructor(header: any = null, track: any = null, align: any = 'start', sortable: any = false) { this.header = header; this.track = track; this.align = align; this.sortable = sortable; } equals(o: unknown) { return o instanceof DataColumn && $eq.equals(this.header, o.header) && $eq.equals(this.track, o.track) && $eq.equals(this.align, o.align) && $eq.equals(this.sortable, o.sortable); } with(patch: any) { return new DataColumn(('header' in patch ? patch.header : this.header), ('track' in patch ? patch.track : this.track), ('align' in patch ? patch.align : this.align), ('sortable' in patch ? patch.sortable : this.sortable)); } toString() { return `DataColumn { Header = ${this.header}, Track = ${this.track}, Align = ${this.align}, Sortable = ${this.sortable} }`; } }

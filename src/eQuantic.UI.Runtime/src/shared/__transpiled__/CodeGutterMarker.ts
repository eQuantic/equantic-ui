import { $eq, CodeGutterKindValue } from "@equantic/runtime";

export class CodeGutterMarker { declare line: number; declare kind: CodeGutterKindValue; declare tooltip: string | null; constructor(line: any = 0, kind: any = null, tooltip: any = null) { this.line = line; this.kind = kind; this.tooltip = tooltip; } equals(o: unknown) { return o instanceof CodeGutterMarker && $eq.equals(this.line, o.line) && $eq.equals(this.kind, o.kind) && $eq.equals(this.tooltip, o.tooltip); } with(patch: any) { return new CodeGutterMarker(('line' in patch ? patch.line : this.line), ('kind' in patch ? patch.kind : this.kind), ('tooltip' in patch ? patch.tooltip : this.tooltip)); } toString() { return `CodeGutterMarker { Line = ${this.line}, Kind = ${this.kind}, Tooltip = ${this.tooltip} }`; } }

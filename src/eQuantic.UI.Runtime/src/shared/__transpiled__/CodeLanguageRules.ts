import { $eq } from "@equantic/runtime";

export class CodeLanguageRules { declare lineComment: string | null; declare blockComment: [string, string] | null; declare brackets: [string, string][]; declare quotes: string[]; declare indentAfter: string[]; declare outdentOn: string[]; declare indentWidth: number; declare insertSpaces: boolean; constructor(lineComment: any = null, blockComment: any = null, brackets: any = [['(', ')'], ['[', ']'], ['{', '}']], quotes: any = ['"', '\''], indentAfter: any = ['{', '(', '['], outdentOn: any = ['}', ')', ']'], indentWidth: any = 4, insertSpaces: any = true) { this.lineComment = lineComment; this.blockComment = blockComment; this.brackets = brackets; this.quotes = quotes; this.indentAfter = indentAfter; this.outdentOn = outdentOn; this.indentWidth = indentWidth; this.insertSpaces = insertSpaces; } equals(o: unknown) { return o instanceof CodeLanguageRules && $eq.equals(this.lineComment, o.lineComment) && $eq.equals(this.blockComment, o.blockComment) && $eq.equals(this.brackets, o.brackets) && $eq.equals(this.quotes, o.quotes) && $eq.equals(this.indentAfter, o.indentAfter) && $eq.equals(this.outdentOn, o.outdentOn) && $eq.equals(this.indentWidth, o.indentWidth) && $eq.equals(this.insertSpaces, o.insertSpaces); } with(patch: any) { return new CodeLanguageRules(('lineComment' in patch ? patch.lineComment : this.lineComment), ('blockComment' in patch ? patch.blockComment : this.blockComment), ('brackets' in patch ? patch.brackets : this.brackets), ('quotes' in patch ? patch.quotes : this.quotes), ('indentAfter' in patch ? patch.indentAfter : this.indentAfter), ('outdentOn' in patch ? patch.outdentOn : this.outdentOn), ('indentWidth' in patch ? patch.indentWidth : this.indentWidth), ('insertSpaces' in patch ? patch.insertSpaces : this.insertSpaces)); } static default = new CodeLanguageRules(); toString() { return `CodeLanguageRules { LineComment = ${this.lineComment}, BlockComment = ${this.blockComment}, Brackets = ${this.brackets}, Quotes = ${this.quotes}, IndentAfter = ${this.indentAfter}, OutdentOn = ${this.outdentOn}, IndentWidth = ${this.indentWidth}, InsertSpaces = ${this.insertSpaces} }`; } }

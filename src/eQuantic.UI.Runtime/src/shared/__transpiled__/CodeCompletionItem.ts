import { $eq, CodeRange } from "@equantic/runtime";

export class CodeCompletionItem { declare label: string; declare kind: string; declare insertText: string | null; declare detail: string | null; declare documentation: string | null; declare sortText: string | null; declare replacing: CodeRange | null; constructor(label: any = null, kind: any = 'text', insertText: any = null, detail: any = null, documentation: any = null, sortText: any = null, replacing: any = null) { this.label = label; this.kind = kind; this.insertText = insertText; this.detail = detail; this.documentation = documentation; this.sortText = sortText; this.replacing = replacing; } equals(o: unknown) { return o instanceof CodeCompletionItem && $eq.equals(this.label, o.label) && $eq.equals(this.kind, o.kind) && $eq.equals(this.insertText, o.insertText) && $eq.equals(this.detail, o.detail) && $eq.equals(this.documentation, o.documentation) && $eq.equals(this.sortText, o.sortText) && $eq.equals(this.replacing, o.replacing); } with(patch: any) { return new CodeCompletionItem(('label' in patch ? patch.label : this.label), ('kind' in patch ? patch.kind : this.kind), ('insertText' in patch ? patch.insertText : this.insertText), ('detail' in patch ? patch.detail : this.detail), ('documentation' in patch ? patch.documentation : this.documentation), ('sortText' in patch ? patch.sortText : this.sortText), ('replacing' in patch ? patch.replacing : this.replacing)); } toString() { return `CodeCompletionItem { Label = ${this.label}, Kind = ${this.kind}, InsertText = ${this.insertText}, Detail = ${this.detail}, Documentation = ${this.documentation}, SortText = ${this.sortText}, Replacing = ${this.replacing} }`; } }
